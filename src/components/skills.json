{
  "hungry": [
    {
      "build": "Create a Svelte UI for a when2meet app",
      "deploy": "Set up continuous deployment with GitHub Actions"
    },
    {
      "build": "Create a React UI for a multi-way chat system",
      "deploy": "Set up an Nginx reverse proxy to host the app"
    },
    {
      "build": "Create a mobile app for garden tracking with React Native Expo",
      "deploy": "Integrate push notifications using Firebase Cloud Messaging"
    },
    {
      "build": "Create a dashboard UI for monitoring server metrics with Vue.js",
      "deploy": "Set up Prometheus and Grafana for real-time monitoring"
    },
    {
      "build": "Create a desktop app for task management with Electron",
      "deploy": "Package the app for Windows, macOS, and Linux"
    }
  ],
  "foolish": [
    {
      "build": "Create a backend API for a Medium-like platform with FastAPI",
      "deploy": "Deploy the API to Kubernetes using a Docker container"
    },
    {
      "build": "Create a backend API for a URL Shortener with Flask",
      "deploy": "Write a load test suite in JS with different request patterns"
    },
    {
      "build": "Create a backend API for a Twitter-style platform with Express.js",
      "deploy": "Implement a Redis cache layer to optimize performance"
    },
    {
      "build": "Create a backend API for a blogging platform with Next.js",
      "deploy": "Implement a rate limiter layer in JS"
    },
    {
      "build": "Create a React UI for a social networking platform",
      "deploy": "Use Jest to write unit and integration tests"
    },

    {
      "build": "Create a Vue UI for a personalized user diary",
      "deploy": "Add authentication using OAuth or BetterAuth"
    },
    {
      "build": "Create a kanban app with React Native",
      "deploy": "Implement offline support with AsyncStorage"
    },
    {
      "build": "Create an Angular UI for a project management tool",
      "deploy": "Set up a smooth infinite scroll that fetches data"
    },
    {
      "build": "Create a on-the-go travel app with SwiftUI",
      "deploy": "Integrate AR using ARKit"
    },
    {
      "build": "Create a computer-use AI agent using LangChain",
      "deploy": "Use uv for dependency management"
    },
    {
      "build": "Create a searchable notes RAG app using Sentence Transformers embeddings",
      "deploy": "Use Pinecone as the vector database"
    }
  ]
}
